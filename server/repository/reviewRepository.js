import { db } from './db.js'

export const getReview = async (formData) => {
    // const sql = `
    //     select * from casetibuy_review where product_id = ?
    // `;
    // const values = [
    //     formData.product_id, // product_id 컬럼
    // ];
    // const [result] = await db.execute(sql, values);

    return result;
}

export const addReview = async (formData) => {
    // const sql = `
    //     insert into casetibuy_review (product_id, member_id, review, rating, created_at)
    //     values (?, ?, ?, ?, now())
    // `;
    // const values = [
    //     formData.product_id,
    //     formData.member_id,
    //     formData.review,
    //     formData.rating,
    // ];
    // const [result] = await db.execute(sql, values);

    // return result;
}